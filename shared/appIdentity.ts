/** Runtime display identity for this local installation. */
export const APP_DISPLAY_NAME = 'MSI System Helper';

/** Keep existing user data in place when the display name changes. */
export const LEGACY_USER_DATA_DIR_NAME = 'MeetingCopilot';
